package router

const (
	// timeoutWarmupPublicMessage explains that one timed-out LSP request may still succeed after server startup settles.
	timeoutWarmupPublicMessage = "request timed out, the LSP server may still be warming up"
)
